# ============================================================
# 智己多智能体知识图谱工作台 — 部署依赖
# Python 3.10 / 3.11 推荐（PaddleOCR 兼容性最佳）
# 安装：pip install -r requirements.txt
# ============================================================

# --- 前端 ---
streamlit>=1.35.0,<1.60.0

# --- Excel 处理 ---
pandas>=2.0.0
openpyxl>=3.1.0

# --- PDF 流水线 (OCR + LLM) ---
openai>=1.14.0           # DashScope 兼容 API 客户端
Pillow>=10.3.0           # 图片处理
paddlepaddle==2.6.2      # OCR 引擎（CPU 版；GPU 版用 paddlepaddle-gpu）
paddleocr==2.7.3         # OCR 文字识别

# --- NHTSA 数据采集 ---
requests>=2.28.0
numpy>=1.24.0,<2.0.0

# --- 图谱可视化 ---
streamlit-agraph>=0.0.45

# --- 可选依赖 ---
# neo4j>=5.19.0          # Neo4j 图数据库（当前仅 legacy 代码使用）
